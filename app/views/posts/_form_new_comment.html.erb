<%= form_with model: Comment.new, local: true, url: add_comment_path(@user.id, @post.id), 
html: {class: "column"} do |f| %>
  <%= f.text_area :text, placeholder: "Comment ...", required: true %>
  <%= f.submit "Add Comment" %>
<% end %>