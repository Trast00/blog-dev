<main class="flex-center column">
  <h2>Create a new Post</h2>
  <% if flash.now[:error].present? %>
    <span class="error-message"><%= flash.now[:error] %></span>
  <% end %>
  <%= render partial: "posts/form_new_post", locals: { post: @post } %>
</main>