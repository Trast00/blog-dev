<%= form_with model: Comment.new, local: true, url: "/users/#{@user.id}/posts/#{@post.id}/add_comment", 
html: {class: "column"} do |f| %>
  <%= f.text_area :text, placeholder: "Comment ...", required: true %>
  <%= f.submit "Add Comment" %>
<% end %>